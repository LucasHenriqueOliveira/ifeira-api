version: "3.1"

services:
  mongo:
    image: mongo
    restart: always
    ports:
      - 8888:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: ifeira
      MONGO_INITDB_ROOT_PASSWORD: 1f3i#a
